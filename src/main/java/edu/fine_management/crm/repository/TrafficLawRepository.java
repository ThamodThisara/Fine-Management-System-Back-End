package edu.fine_management.crm.repository;

import edu.fine_management.crm.entity.TrafficLawEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TrafficLawRepository extends JpaRepository<TrafficLawEntity,Integer> {
}
